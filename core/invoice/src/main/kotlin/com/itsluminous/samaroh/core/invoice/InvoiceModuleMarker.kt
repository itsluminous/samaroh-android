package com.itsluminous.samaroh.core.invoice

/**
 * Module shell (Wave 0). W1-E implements the `PdfDocument`-based invoice renderer here,
 * following the layout contract in shared/invoice/layout-spec.md (localized text,
 * `{prefix}-{YYYY}-{counter:04d}` immutable numbering, Indian digit grouping).
 */
object InvoiceModuleMarker
