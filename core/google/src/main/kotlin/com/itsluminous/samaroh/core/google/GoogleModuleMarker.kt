package com.itsluminous.samaroh.core.google

/**
 * Module shell (Wave 0). W1-F implements Google account linking via Credential Manager,
 * Drive uploads/backup ZIPs (`drive.file` scope, §9.1 layout) and one-way Calendar sync
 * (`calendar.events` scope) here.
 */
object GoogleModuleMarker
