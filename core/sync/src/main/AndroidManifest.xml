<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Conflict notifications (§8). Optional permission — the app is fully usable without it (§6). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Expedited sync runs as a dataSync foreground task before Android S. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application>
        <!-- Registers the app-foreground sync trigger without any :app wiring. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="com.itsluminous.samaroh.core.sync.SyncStartupInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- WorkManager's foreground service needs an explicit type on API 34+. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
